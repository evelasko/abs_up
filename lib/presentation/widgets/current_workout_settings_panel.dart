import 'package:flutter/material.dart';

class CurrentWorkoutsettingsPanel extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('current workout settings panel'),
    );
  }
}
